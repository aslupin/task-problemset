using namespace std;
#include <iostream>
#include <algorithm>
#include <vector>
main(){
    vector<int> con;
    int a=6,b=9,c=20,max_var=6;
    int n,i=0;
    cin >> n;
    do{
        
        if(max_var < a || max_var < b || max_var < c)
        
    }while(max_var <= n);
    }

